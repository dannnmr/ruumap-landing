# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*
!.env.example

# vercel
.vercel

# openspec: planning/spec artifacts for AI-assisted development — local only,
# never meant for the shared repo or production (Next.js never builds/serves
# these files regardless, but they're excluded here too since collaborators
# with repo access shouldn't see Claude's internal planning process either).
openspec/

# typescript
*.tsbuildinfo
next-env.d.ts